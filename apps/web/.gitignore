.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright. The saved session is a real credential for the local fixture and
# is regenerated by e2e/auth.setup.ts on every run — never commit it.
/e2e/.auth/
/test-results/
/playwright-report/
/blob-report/

# Turborepo + SvelteKit need some packages hoisted to the root node_modules:
# Vite resolves plugins from the app's own tree, and patch-package (used by the
# web app) walks node_modules from its CWD.
node-linker=hoisted
engine-strict=true

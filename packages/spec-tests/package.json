{
  "name": "@kaaj/spec-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spec-derived conformance tests and golden fixtures",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:security": "vitest run tests/security-invariants.spec.test.ts tests/security-operational.spec.test.ts tests/security-traceability.spec.test.ts",
    "test:watch": "vitest",
    "format:check": "prettier --check README.md src requirements fixtures tests tsconfig.json package.json",
    "format": "prettier --write README.md src requirements fixtures tests tsconfig.json package.json"
  },
  "devDependencies": {
    "@kaaj/typescript-config": "workspace:*",
    "@types/node": "^22.13.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.7"
  }
}
